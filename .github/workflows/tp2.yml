name: tp2 compilation
on:
  push:
    branches:
      - tp2
jobs:
  job1:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
    - uses: actions/checkout@v2
    - name: affichage
      run: |
        echo $OSTYPE

